{
	"name": "lottocheck",
	"version": "1.0.0",
	"description": "CloudFlare Worker to check lottery jackpots and send notifications",
	"main": "src/index.js",
	"type": "module",
	"scripts": {
		"build": "echo 'no build step'",
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint .",
		"format": "prettier --write .",
		"format:check": "prettier --check ."
	},
	"overrides": {
		"undici": "^7.28.0",
		"picomatch": "^4.0.4"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.19.0",
		"@eslint/js": "^10.0.1",
		"eslint": "^10.8.0",
		"globals": "^17.8.0",
		"prettier": "3.9.6",
		"vitest": "^4.1.10",
		"wrangler": "^4.77.0"
	}
}
